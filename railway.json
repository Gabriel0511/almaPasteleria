{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd alma-backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd alma-backend && python manage.py migrate && gunicorn mysite.wsgi:application --bind 0.0.0.0:$PORT"
  }
}