[build]
builder = "nixpacks"
nixPkgs = ["pkg-config", "mysql.dev", "gcc"]

[phases.install]
commands = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[deploy]
startCommand = "python manage.py migrate && gunicorn tu_proyecto.wsgi --bind 0.0.0.0:$PORT"